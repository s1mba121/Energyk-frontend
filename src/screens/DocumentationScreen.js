import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DocumentationScreen = () => {
    return (
        <View>
            <Text>DocumentationScreen</Text>
        </View>
    );
};

export default DocumentationScreen;

const styles = StyleSheet.create({});
